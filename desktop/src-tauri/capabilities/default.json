{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "core window and event access, artifact and log-export dialogs, the clipboard, url opening and oauth callbacks, launch-at-login, and self-update",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "autostart:default",
    "clipboard-manager:allow-write-text",
    "dialog:allow-open",
    "dialog:allow-save",
    "deep-link:default",
    "opener:default",
    "updater:default"
  ]
}
